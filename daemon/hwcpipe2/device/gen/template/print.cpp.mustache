{{> copyright}}

#include "print.hpp"

#include <debug/ostream_indent.hpp>
#include <debug/print_array.hpp>

namespace hwcpipe {
namespace device {
namespace ioctl {
namespace {{name}} {

{{#items}}
{{> print}}
{{/items}}
std::ostream &operator<<(std::ostream &os, command::command_type cmd) {
    switch (cmd) {
{{#commands}}
    case command::{{name}}:
        return os << "{{parent.name}}::command::{{name}}";
{{/commands}}
    default:
        assert(!"Unknown command");
        return os << "{{parent.name}}::command::<unknown>";
    }
}

} // namespace {{name}}
} // namespace ioctl
} // namespace device
} // namespace hwcpipe
